import * as React from 'react';
import { Text, View } from 'react-native';

// Displays a list of decks with the no of cards in them.

const Decks = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Decks</Text>
    </View>
  );
};

export default Decks;
